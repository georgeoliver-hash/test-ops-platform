# ETM consolidation — retire the sequential sign-on step-cases (folded into the full-flow test).
# Creates a "ZZ - To Delete" section as George's move-target, and renames the superseded cases to
# ZZ_DELETE_REVIEW so they sort together and are easy to drag into it (the API cannot move/delete).
# One-shot spec — do NOT re-push (the match titles only exist before the first rename).
suite_id: 30254
defaults:
  template_id: 1
  custom_autoconfirmation: false
  custom_devtypes: [25]
  custom_revstatus: 2

sections:
  - path: ZZ - To Delete (review then bin)
    cases: []
  - path: Functional / Sign On & Session / Driver
    cases:
      - title: "ZZ_DELETE_REVIEW - Driver Sign On — duty number entry (folded into full flow)"
        match: Driver Sign On — duty number entry and correction
        objective: This test is to confirm superseded — folded into the Driver Sign On full-flow test.
        steps: |
          **Given** superseded
          **WHEN** superseded
          **THEN** superseded
        expected: Superseded — folded into "Driver Sign On — Manual, first use".
      - title: "ZZ_DELETE_REVIEW - Driver Sign On — route selection (folded into full flow)"
        match: Driver Sign On — route selection
        objective: This test is to confirm superseded — folded into the Driver Sign On full-flow test.
        steps: |
          **Given** superseded
          **WHEN** superseded
          **THEN** superseded
        expected: Superseded — folded into "Driver Sign On — Manual, first use".
      - title: "ZZ_DELETE_REVIEW - Driver Sign On — journey selection (folded into full flow)"
        match: Driver Sign On — journey selection and confirmation
        objective: This test is to confirm superseded — folded into the Driver Sign On full-flow test.
        steps: |
          **Given** superseded
          **WHEN** superseded
          **THEN** superseded
        expected: Superseded — folded into "Driver Sign On — Manual, first use".
      - title: "ZZ_DELETE_REVIEW - Driver Sign On — Message of the Day (folded into full flow)"
        match: Driver Sign On — Message of the Day
        objective: This test is to confirm superseded — folded into the Driver Sign On full-flow test.
        steps: |
          **Given** superseded
          **WHEN** superseded
          **THEN** superseded
        expected: Superseded — folded into "Driver Sign On — Manual, first use".
      - title: "ZZ_DELETE_REVIEW - Driver Sign On — Word and Colours of the Day (folded into full flow)"
        match: Driver Sign On — Word & Colours of the Day
        objective: This test is to confirm superseded — folded into the Driver Sign On full-flow test.
        steps: |
          **Given** superseded
          **WHEN** superseded
          **THEN** superseded
        expected: Superseded — folded into "Driver Sign On — Manual, first use".
