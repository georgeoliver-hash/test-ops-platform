# Sign On & Session — drafts (superseded)

The initial 9 Operator-only drafts here have been **superseded** by the full consolidated set built
from the real old suite + all flows + the TIBU defect backlog:

- **`sign-on-mapping.md`** — the old → new analysis (consolidation decisions, bugs folded, gaps, traceability). *Start here.*
- **`sign-on.cases.yaml`** — the 21 consolidated `@mode(all)` cases, pushable to the new suite:
  `python -m system_test_ops push --file proposals/pos-suite-restructure/sign-on.cases.yaml` (dry-run; `--commit` to create).
