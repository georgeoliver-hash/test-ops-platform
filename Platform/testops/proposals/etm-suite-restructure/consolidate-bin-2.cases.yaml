# ETM consolidation pass ? retire the 30 absorbed cases (folded into keepers). One-shot.
suite_id: 30254
defaults:
  template_id: 1
  custom_autoconfirmation: false
  custom_devtypes:
  - 25
  custom_revstatus: 2
sections:
- path: Functional / Fare Look-Up / Navigation
  cases:
  - title: ZZ_DELETE_REVIEW - FLU ? change boarding stage manually (folded)
    match: FLU ? change boarding stage manually
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - FLU ? journey type toggle (folded)
    match: FLU ? journey type toggle
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - FLU ? preset and menu-type products (folded)
    match: FLU ? preset and menu-type products
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - FLU ? selection times out to the default product (folded)
    match: FLU ? selection times out to the default product
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Fare Look-Up / Sales
  cases:
  - title: ZZ_DELETE_REVIEW - FLU ? last transaction display and success timeout (folded)
    match: FLU ? last transaction display and success timeout
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - FLU ? default ticket class reverts on timeout (folded)
    match: FLU ? default ticket class reverts on timeout
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - FLU ? Easibus product menu (folded)
    match: FLU ? Easibus product menu
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Ticket Issue
  cases:
  - title: ZZ_DELETE_REVIEW - Ticket Issue ? annulment times out (folded)
    match: Ticket Issue ? annulment times out
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Ticket Issue ? annulment refused for non-annullable transactions (folded)
    match: Ticket Issue ? annulment refused for non-annullable transactions
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Ticket Issue ? issue with a change receipt (folded)
    match: Ticket Issue ? issue with a change receipt
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Ticket Issue / Promo Menu
  cases:
  - title: ZZ_DELETE_REVIEW - Promo Menu ? add an additional child to a family product (folded)
    match: Promo Menu ? add an additional child to a family product
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Promo Menu ? basket tracker persists in sub-menus (folded)
    match: Promo Menu ? basket tracker persists in sub-menus
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Basket Mode
  cases:
  - title: ZZ_DELETE_REVIEW - Basket Mode ? change line-item quantity (folded)
    match: Basket Mode ? change line-item quantity
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Smartcards / Validation
  cases:
  - title: ZZ_DELETE_REVIEW - Smartcard ? direction and transfer-period rules (folded)
    match: Smartcard ? direction and transfer-period rules
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Smartcards / Top-Up
  cases:
  - title: ZZ_DELETE_REVIEW - Smartcard top-up ? maximum journeys enforced (folded)
    match: Smartcard top-up ? maximum journeys enforced
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Smartcard top-up ? expired journeys removed (folded)
    match: Smartcard top-up ? expired journeys removed
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / ABT
  cases:
  - title: ZZ_DELETE_REVIEW - ABT ? passback on second tap (folded)
    match: ABT ? passback on second tap
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - ABT ? mobile wallet taps (folded)
    match: ABT ? mobile wallet taps
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - ABT ? EMV validation failure variants (folded)
    match: ABT ? EMV validation failure variants
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Driver Menu & Options
  cases:
  - title: ZZ_DELETE_REVIEW - Driver options ? messages (folded)
    match: Driver options ? messages
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Driver options ? Word and Colour of the Day (folded)
    match: Driver options ? Word and Colour of the Day
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Driver options ? paper status (folded)
    match: Driver options ? paper status
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Driver options ? ticket history and last ticket (folded)
    match: Driver options ? ticket history and last ticket
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Driver options ? reboot card reader (folded)
    match: Driver options ? reboot card reader
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - Driver options ? report faulty reader (folded)
    match: Driver options ? report faulty reader
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Supervisor Menu
  cases:
  - title: ZZ_DELETE_REVIEW - Supervisor ? GPS information (folded)
    match: Supervisor ? GPS information
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Technician Menu
  cases:
  - title: ZZ_DELETE_REVIEW - Technician ? view and print versions (folded)
    match: Technician ? view and print versions
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Barcode Scanning
  cases:
  - title: ZZ_DELETE_REVIEW - Barcode ? online validation failures (folded)
    match: Barcode ? online validation failures
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
- path: Functional / Location
  cases:
  - title: ZZ_DELETE_REVIEW - GPS ? arrival and departure at stops (folded)
    match: GPS ? arrival and departure at stops
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
  - title: ZZ_DELETE_REVIEW - GPS ? look ahead (folded)
    match: GPS ? look ahead
    objective: This test is to confirm superseded ? folded into a consolidated flow test.
    steps: '**Given** superseded

      **WHEN** superseded

      **THEN** superseded'
    expected: Superseded ? folded into a consolidated flow test (see consolidation-audit.md).
